import 'package:flutter/material.dart';

class Center extends StatelessWidget {
  const Center({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Text('Welcome to Auto Aid'),
    );
  }
}
